export interface Bucket {
  bucketId: string;
  bucketName: string;
  accessKeyId: string;
  bucket: string;
  endpoint: string;
  key: string;
  secretAccessKey: string;
  region: string;
  active: boolean;
}
