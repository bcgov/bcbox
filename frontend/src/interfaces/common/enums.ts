// Enums that represent general app-wide values (not buisness-specific like objects, buckets, etc)

// Which way to display a button that can appear in multiple modes
export enum ButtonMode {
  BUTTON = 'BUTTON',
  ICON = 'ICON',
}
