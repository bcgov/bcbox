// eslint-disable-next-line
// Just to quiet a no imports error for now
import { ValidationMessages } from '@/utils/constants';

describe('App', () => {
  test('working', () => {
    expect(1).toBeTruthy();
  });
});
